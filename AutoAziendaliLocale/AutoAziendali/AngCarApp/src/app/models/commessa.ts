import { DatePipe } from '@angular/common';

export class Commessa{

    constructor (public IdCommessa: number, public Commessa: string, public IdSocieta: number, public IdBusinessUnit: number){

    }
}