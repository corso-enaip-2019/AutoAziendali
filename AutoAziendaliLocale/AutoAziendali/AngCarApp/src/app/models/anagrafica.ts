import { DatePipe } from '@angular/common';

export class Anagrafica{
    constructor(public IdAnagrafica: number, public Cognome: string, public Nome: string, public IdSocieta: number, public IdBusinessUnit: number){

    }
}